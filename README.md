# SmartClean — Crowd-Powered AI Litter Mapping

## Quick start (local)

1. Clone repo and enter `backend/`

2. Create virtualenv and install:
   ```bash
   python -m venv .venv
   source .venv/bin/activate   # windows: .venv\Scripts\activate
   pip install -r requirements.txt
